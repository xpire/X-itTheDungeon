package main.app.controller;

import main.app.model.AppScreen;

public class ExploreModeController extends AppController {
    public ExploreModeController(AppScreen screen) {
        super(screen);
    }

    @Override
    public void switchScreen(AppScreen newScreen) {
        super.switchScreen(newScreen);
    }
}
