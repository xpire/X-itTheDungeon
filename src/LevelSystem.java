/*
import java.util.ArrayList;

public class LevelSystem {

    private ArrayList<LevelWrapper> levels;

    public void addLevel(String levelName, String fileName) {
        levels.add(new LevelWrapper(levelName, fileName));
    }

    private class LevelWrapper {

        private String levelName;
        private String fileName;

        public LevelWrapper(String levelName, String fileName){
            this.levelName = levelName;
            this.fileName = fileName;
        }
    }

    public boolean isLocked() {
//        return Main.stats.getStat(StatisticType.MAX_LEVEL_CONQUERED).get() < ;
    }
}
*/
